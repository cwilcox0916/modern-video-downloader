# Modern Video Downloader Configuration
# Copy this file to .env and adjust the values for your environment

# =============================================================================
# UNRAID CONFIGURATION (Recommended)
# =============================================================================

# Directory where downloaded videos will be saved on your Unraid server
# This should point to a location on your Unraid array or cache drive
DEFAULT_DOWNLOAD_DIR=/mnt/user/Media/Downloads

# API URL for the frontend to connect to the backend
# Replace <YOUR-UNRAID-IP> with your actual Unraid server IP address
# Example: VITE_API_URL=http://192.168.1.100:8000/
VITE_API_URL=http://<YOUR-UNRAID-IP>:8000/

# =============================================================================
# ALTERNATIVE CONFIGURATIONS
# =============================================================================

# For Docker Compose internal networking (if both containers use same network):
# VITE_API_URL=http://mvd-api:8000/

# For local development on Windows:
# DEFAULT_DOWNLOAD_DIR=C:\\Users\\<username>\\Downloads
# VITE_API_URL=http://localhost:8000/

# For local development on Linux/macOS:
# DEFAULT_DOWNLOAD_DIR=/home/<username>/Downloads
# VITE_API_URL=http://localhost:8000/

# =============================================================================
# NOTES
# =============================================================================
# 
# - DEFAULT_DOWNLOAD_DIR is mounted into the API container at /downloads
# - VITE_API_URL is used by the frontend at build time
# - Make sure the download directory exists and has proper permissions
# - For Unraid, recommended permissions: chmod 755 and chown nobody:users
